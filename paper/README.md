# LaTeX Paper

Generate the current manuscript with:

```bash
python scripts/generate_latex_paper.py
```

The generated paper is written to `paper/fastbpe_paper.tex` and references plots in `results/plots/`.
